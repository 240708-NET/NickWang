using System.Text;

class Program
{
    public static void Main(string[] args)
    {
        int N = 4;
        StringBuilder sb = new StringBuilder();
        var temp = Enumerable.Range(1, 10);
        foreach (var i in temp)
        {
            
        }

        Console.WriteLine(temp.GetType());
    }
}